spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=digital_wallet
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: user-service
